# Batch log
logging.level.org.springframework.batch: info


logging:
  level:
    root: info
    org:
      # SpringFramework Logging
      springframework: info
      # jpa 쿼리를 로그 파일로 출력
      hibernate:
        SQL: info
        # 로그 파라미터까지 확인
#        type: trace

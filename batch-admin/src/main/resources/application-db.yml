spring.profiles: db
spring.profiles.include: db_mysql #db_h2 #, db_oracle

logging:
  level:
    org:
      hibernate:
        type:
          descriptor:

            # DB Query Logging
            sql: debug
